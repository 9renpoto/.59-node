{
  "name": "@app/ui",
  "version": "0.0.1",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc --build .",
    "clean": "rimraf lib",
    "test": "jest"
  },
  "typings": "lib/index.d.ts",
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "collectCoverage": true
  },
  "devDependencies": {
    "@storybook/react": "^6.0.28"
  }
}

{
  "name": "@app/ui",
  "version": "0.0.1",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc --build .",
    "clean": "rimraf lib",
    "test": "jest"
  },
  "devDependencies": {
    "@storybook/react": "6.1.18"
  },
  "typings": "lib/index.d.ts",
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "collectCoverage": true
  }
}
